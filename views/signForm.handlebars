<div id="main-area">
    <div id="signIn-area">
        <h1>Sign In</h1>
        <form action="/create-account" method="POST" autocomplete="off">
            <input type="text" name="username" required placeholder="Your username..." minlength="3"><br>
            <input type="email" name="email" required placeholder="Your email..."><br>
            <input type="password" name="password" required placeholder="Your password..." minlength="5"><br>
            <button type="submit">Sign In</button>
        </form>
    </div>
</div>
<style>
    input {
        height: 40px;
        width: 60%;
        border-radius: 1ch;
        font-size: 20px;
    }

    h1 {
        font-weight: bold;
    }

    button {
        background-color: transparent;
        background-repeat: no-repeat;
        border-radius: 1ch;
        width: 90px;
        height: 40px;
        border: none;
        overflow: hidden;
        outline: none;
        cursor: pointer;
        font-size: 20px;
        font-weight: bold;
        margin: 30px;
        background: rgb(58, 53, 82);
        transition: 0.5s;
        color: white;
    }

    button:hover {
        animation: buttonColor 2s infinite;
    }

    form {
        display: flex;
        flex-direction: column;
        flex-wrap: wrap;
        justify-content: center;
        align-items: center;
        width: 100%;
    }

    #main-area {
        box-sizing: border-box;
        display: flex;
        flex-direction: column;
        align-items: center;
        min-width: 30%;
        height: 100%;
        justify-content: center;
    }

    #signIn-area {
        display: flex;
        flex-direction: column;
        flex-wrap: wrap;
        background-color: rgba(71, 212, 158, 0.651);
        justify-content: center;
        align-items: center;
        box-sizing: border-box;
        border-radius: 2ch;
        width: 100%;
        animation: shadowMove 3.5s infinite;
    }

    @keyframes buttonColor {
        50% {
            background: rgba(58, 53, 82, 0.65);
            border-radius: 1ch;
        }
    }

    @keyframes shadowMove {
        50% {
            box-shadow: 5px 5px rgba(33, 99, 74, 0.651);
        }
    }
</style>